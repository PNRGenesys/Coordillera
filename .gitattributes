# Text files are stored with LF and checked out with LF on every platform,
# so Windows does not rewrite them to CRLF and the editor stops warning about it.
* text=auto eol=lf

# Assets must never be touched by the line ending conversion.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
